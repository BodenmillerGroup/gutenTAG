df <- x
coords <- coords
dim(coords)
matrix_image <- matrix(NA, ncol = max(coords$y), nrow = max(coords$x))
# intensity vector
x <- df[, channel_number]
# get intensity value for 99th percentile most intense pixels
#x_max <- quantile(x, probs = quantile_lim)
x_max <- quantile(x, probs = quantile_lim, na.rm = TRUE)
# set all pixel values greater than x_max to that of x_max
x[x > x_max] <- x_max
matrix_image[as.matrix(coords)] <- x
# convert matrix to image
matrix_image <- as.cimg(matrix_image - min(matrix_image, na.rm = TRUE))
# add colour channels to the image
matrix_image <- add.color(matrix_image, simple = TRUE)
# set red and blue channels to zero to get only green
R(matrix_image) <- 0
B(matrix_image) <- 0
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,0,0,0))
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(1,1,1,1))
axes = TRUE
interpolate = FALSE
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(1,1,1,1))
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,1,1,1))
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,0,1,1))
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,0,0,1))
col.na = "white"
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = if(col.na = "black"{
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = if(col.na == "black"{
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = if(col.na == "black"){
rgb(0,0,0,1))
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = if(col.na == "black"){
rgb(0,0,0,1)
}if(col.na == "white"){
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = if(col.na == "black"){
rgb(0,0,0,1)
}
if(col.na == "white"){
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = if(col.na == "black"){
rgb(0,0,0,1)
}if(col.na == "white"){
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
if(col.na == "black"){
col.na = rgb(0,0,0,1)
}
if(col.na == "white"){
colna = "white"
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
if(colna == "black"){
col.na = rgb(0,0,0,1)
}
if(colna == "white"){
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
if(colna == "black"){
col.na = rgb(0,0,0,1)
}if(colna == "white"){
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
if(colna == "black"){
col.na = rgb(0,0,0,1)
}
#if(colna == "white"){
#  col.na = rgb(0,0,0,0)
#  }
)
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
if(colna == "black"){
col.na = rgb(0,0,0,1)
}else(colna == "white"){
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
if(colna == "black"){
col.na = rgb(0,0,0,1)
}else{
col.na = rgb(0,0,0,0)
}
)
colna = "black"
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
if(colna == "black"){
col.na = rgb(0,0,0,1)
}else{
col.na = rgb(0,0,0,0)
}
)
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
if(colna == "black"){
col.na = rgb(0,0,0,1)
}else{
col.na = rgb(0,0,0,0)
}
)
#plot(matrix_image, main = colnames(df)[channel_number])
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
if(colna == "black"){
col.na = rgb(0,0,0,1)
}
)
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,0,0,1))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,0,0,1))
colna = "white"
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,0,0,1))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
#col.na = rgb(0,0,0,1))
ifelse(colna = "black", col.na = rgb(0,0,0,1), col.na = rgb(0,0,0,0))
}
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
#col.na = rgb(0,0,0,1))
ifelse(colna = "black", col.na = rgb(0,0,0,1), col.na = rgb(0,0,0,0)))
source("~/github/maldi-imaging/R/imageChannel.R", echo=TRUE)
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
#col.na = rgb(0,0,0,1))
col.na = ifelse(colna = "black", rgb(0,0,0,1), rgb(0,0,0,0)))
colna = "black"
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
#col.na = rgb(0,0,0,1))
col.na = ifelse(colna = "black", rgb(0,0,0,1), rgb(0,0,0,0)))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,0,0,0))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,0,1,0))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,1,0,0))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(1,1,0,0))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(1,1,1,0))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(1,1,1,1))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,1,1,1))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(1,0,1,1))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(1,0,0,1))
plot(matrix_image, main = colnames(df)[channel_number],
interpolate = interpolate,
xlim = c(1, max(coords$x)), ylim = c(max(coords$y), 1),
axes = axes,
col.na = rgb(0,0,0,1))
devtools::document()
rm(imageChannel())
rm(imageChannel
)
devtools::document()
imageChannel(final_filtered, final$SpatialCoords, 2)
imageChannel(final_filtered, final$SpatialCoords, 3)
imageChannel(final_filtered, final$SpatialCoords, 5)
imageChannel(final$IntensityDF, final$SpatialCoords, 5)
imageChannel(final$IntensityDF, final$SpatialCoords, 6)
imageChannel(final_filtered, final$SpatialCoords, 6)
imageChannel(final$IntensityDF, final$SpatialCoords, 7)
imageChannel(final_filtered, final$SpatialCoords, 7)
final_filtered
# 1. Build adjacency matrix
KNN_graph_matrix <- N2R::Knn(as.matrix(final_filtered), k = 30, verbose = FALSE, indexType = "angular")
final_filtered[!is.na(final_filtered)]
final_filtered[1]
final_filtered[1][!is.na(final_filtered[1])]
filtered <- c()
for (i in 1:ncol(final_filtered)){
tmp <- final_filtered[i][!is.na(final_filtered[i])]
filtered <- cbind(filtered, tmp)
}
filtered
dim(filtered)
colnames(filtered) <- colnames(final_filtered)
filtered
filtered <- data.frame(filtered)
filtered
dim(filtered)
!is.na(final_filtered[i]
)
length(!is.na(final_filtered[i]))
length(final_filtered[i][!is.na(final_filtered[i])])
filtered
# 1. Build adjacency matrix
KNN_graph_matrix <- N2R::Knn(as.matrix(final_filtered), k = 30, verbose = FALSE, indexType = "angular")
# filter coordinates
final_filtered %in% filtered
final_filtered
# filter coordinates
final_filtered[1] %in% filtered[1]
# filter coordinates
final_filtered[filtered]
filtered
# filter coordinates
final_filtered[1][filtered[1]]
filtered[1]
final_filtered[1]
# 1. Build adjacency matrix
KNN_graph_matrix <- N2R::Knn(as.matrix(filtered), k = 30, verbose = FALSE, indexType = "angular")
KNN_graph_matrix <- KNN_graph_matrix + t(KNN_graph_matrix)
# 2. Build graph from adjacency matrix
Final_graph <- graph_from_adjacency_matrix(KNN_graph_matrix, mode = 'undirected', weighted = TRUE)
# 2. Build graph from adjacency matrix
Final_graph <- igraph::graph_from_adjacency_matrix(KNN_graph_matrix, mode = 'undirected', weighted = TRUE)
# 3. Perform louvain clustering
clustering <- cluster_louvain(Final_graph)
# 3. Perform louvain clustering
clustering <- igraph::cluster_louvain(Final_graph)
# get membership for each pixel
cluster <- as.character(membership(clustering))
# get membership for each pixel
cluster <- as.character(igraph::membership(clustering))
# get number of clusters
n_clusters <- length(unique(cluster))
n_clusters
# get only coordinates of non-NA value pixels
coords <- final$SpatialCoords
coords
filtered
cluster_coords <- cbind(coords, cluster)
cluster_coords
cluster
coords
tmp
final_filtered[i][!is.na(final_filtered[i])]
is.na(final_filtered[i])
which(is.na(final_filtered[i]))
indices <- which(is.na(final_filtered[i]))
coords[indices]
coords
coords[indices]
coords[indices, ]
final_filtered[i][!is.na(final_filtered[i]), ]
final_filtered[!is.na(final_filtered), ]
filtered_coords <- coords[indices, ]
filtered_coords
indices <- which(!is.na(final_filtered[i]))
filtered_coords <- coords[indices, ]
filtered_coords
dim(filtered)
dim(filtered_coords)
cluster
# filtered clusters with coordinates
cluster_coords <- cbind(filtered_coords, cluster)
cluster_coords
# ggplot spatial distribution of clusters
ggplot(cluster_coords, aes(x = x, y = y, color = Clustering_group)) +
geom_point(size = 1) +
scale_color_discrete(name="Cluster Membership") +
theme_minimal() +
labs(title = "Cluster Memberships on Spatial Coordinates",
x = "X Coordinate",
y = "Y Coordinate")
# ggplot spatial distribution of clusters
ggplot2::ggplot(cluster_coords, aes(x = x, y = y, color = Clustering_group)) +
geom_point(size = 1) +
scale_color_discrete(name="Cluster Membership") +
theme_minimal() +
labs(title = "Cluster Memberships on Spatial Coordinates",
x = "X Coordinate",
y = "Y Coordinate")
library(ggplot2)
# ggplot spatial distribution of clusters
ggplot(cluster_coords, aes(x = x, y = y, color = Clustering_group)) +
geom_point(size = 1) +
scale_color_discrete(name="Cluster Membership") +
theme_minimal() +
labs(title = "Cluster Memberships on Spatial Coordinates",
x = "X Coordinate",
y = "Y Coordinate")
# ggplot spatial distribution of clusters
ggplot(cluster_coords, aes(x = x, y = y, color = cluster)) +
geom_point(size = 1) +
scale_color_discrete(name="Cluster Membership") +
theme_minimal() +
labs(title = "Cluster Memberships on Spatial Coordinates",
x = "X Coordinate",
y = "Y Coordinate")
# 1. Build adjacency matrix
KNN_graph_matrix <- N2R::Knn(as.matrix(filtered), k = 100, verbose = FALSE, indexType = "angular")
KNN_graph_matrix <- KNN_graph_matrix + t(KNN_graph_matrix)
# 2. Build graph from adjacency matrix
Final_graph <- igraph::graph_from_adjacency_matrix(KNN_graph_matrix, mode = 'undirected', weighted = TRUE)
# 3. Perform louvain clustering
clustering <- igraph::cluster_louvain(Final_graph)
# get membership for each pixel
cluster <- as.character(igraph::membership(clustering))
# filter coordinates
coords <- final$SpatialCoords
indices <- which(!is.na(final_filtered[i]))
coords <- final$SpatialCoords
filtered_coords <- coords[indices, ]
# filtered clusters with coordinates
cluster_coords <- cbind(filtered_coords, cluster)
# ggplot spatial distribution of clusters
ggplot(cluster_coords, aes(x = x, y = y, color = cluster)) +
geom_point(size = 1) +
scale_color_discrete(name="Cluster Membership") +
theme_minimal() +
labs(title = "Cluster Memberships on Spatial Coordinates",
x = "X Coordinate",
y = "Y Coordinate")
# 1. Build adjacency matrix
KNN_graph_matrix <- N2R::Knn(as.matrix(filtered), k = 200, verbose = FALSE, indexType = "angular")
KNN_graph_matrix <- KNN_graph_matrix + t(KNN_graph_matrix)
# 2. Build graph from adjacency matrix
Final_graph <- igraph::graph_from_adjacency_matrix(KNN_graph_matrix, mode = 'undirected', weighted = TRUE)
# 3. Perform louvain clustering
clustering <- igraph::cluster_louvain(Final_graph)
# get membership for each pixel
cluster <- as.character(igraph::membership(clustering))
# get number of clusters
n_clusters <- length(unique(cluster))
# filter coordinates
coords <- final$SpatialCoords
indices <- which(!is.na(final_filtered[i]))
coords <- final$SpatialCoords
filtered_coords <- coords[indices, ]
# filtered clusters with coordinates
cluster_coords <- cbind(filtered_coords, cluster)
# ggplot spatial distribution of clusters
ggplot(cluster_coords, aes(x = x, y = y, color = cluster)) +
geom_point(size = 1) +
scale_color_discrete(name="Cluster Membership") +
theme_minimal() +
labs(title = "Cluster Memberships on Spatial Coordinates",
x = "X Coordinate",
y = "Y Coordinate")
louvain_cluster <- function(x, coords, k = 300, metric = "angular", plot = FALSE, filter = FALSE, resolution = 0.5){
if(is.null(dim(x))){
intensity <- x$IntensityDF
}else{
intensity <- x
}
# filter out by TIC
if(filter == TRUE){
intensity <- intensity[!is.na(intensity), ]
}
# 1. Build adjacency matrix
KNN_graph_matrix <- N2R::Knn(as.matrix(intensity), k = k, verbose = FALSE, indexType = metric)
KNN_graph_matrix <- as(KNN_graph_matrix, "dgCMatrix")
# transpose matrix and make sparse
t_KNN_graph_matrix <- Matrix::t(KNN_graph_matrix)
#t_KNN_graph_matrix <- as(t_KNN_graph_matrix, "dgCMatrix")
KNN_graph_matrix <- KNN_graph_matrix + t_KNN_graph_matrix
# 2. Build graph from adjacency matrix
graph <- igraph::graph_from_adjacency_matrix(KNN_graph_matrix, mode = 'undirected', weighted = TRUE)
# 3. Perform louvain clustering)
clustering <- igraph::cluster_louvain(graph, resolution = resolution)
# get membership for each pixel
membership <- as.character(igraph::membership(clustering))
# bind cluster membership to pixel coordinates
coords <- x$SpatialCoords
# subset filtered coordinates
if (filter == TRUE){
indices <- which(!is.na(intensity[i]))
coords <- coords[indices, ]
}
cluster_coords <- cbind(coords, membership)
if(plot == TRUE){
# ggplot spatial distribution of clusters
ggplot(cluster_coords, aes(x = x, y = y, color = membership)) +
geom_point(size = 1) +
#scale_color_discrete(name = "Cluster Membership") +
scale_color_brewer(palette = "Set1", name = "Cluster Membership") +
theme_minimal() +
labs(title = "Cluster Memberships on Spatial Coordinates",
x = "X Coordinate",
y = "Y Coordinate")
}
print(length(unique(membership)))
return(membership)
}
louvain_cluster(final_filtered, coords = final$SpatialCoords, k = 300, filter = TRUE)
