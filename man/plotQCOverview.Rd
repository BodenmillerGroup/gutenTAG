% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotQC.R
\name{plotQCOverview}
\alias{plotQCOverview}
\title{Plot Geary's C vs SNR Overview ('Trashcan Plot')}
\usage{
plotQCOverview(
  x,
  geary_threshold = 0.7,
  snr_threshold = 3,
  interactive = FALSE
)
}
\arguments{
\item{x}{List output of \code{computeSNR(..., update_correspondence = TRUE)}
where \code{computeGearysC(..., update_correspondence = TRUE)} has also been
run, so \code{CorrespondenceMatrix} contains both \code{GearysC} and
\code{snr} columns.}

\item{geary_threshold}{Numeric; Geary's C cutoff for the trashcan zone.
Default \code{0.7}.}

\item{snr_threshold}{Numeric; SNR cutoff for the trashcan zone.
Default \code{3}.}

\item{interactive}{Logical; if \code{TRUE} returns a \code{plotly} object.
Requires the \pkg{plotly} package. Default \code{FALSE}.}
}
\value{
A \code{ggplot} object, or a \code{plotly} object when
  \code{interactive = TRUE}.
}
\description{
2D scatter of Geary's C vs SNR per marker. A shaded red
  rectangle marks the low-quality zone (Geary's C > \code{geary_threshold}
  AND SNR < \code{snr_threshold}). Points are labelled using
  \pkg{ggrepel}.
}
\examples{
\donttest{
  rdata_path <- system.file("extdata/Example_processed.Rdata", package = "gutenTAG")
  load(rdata_path)
  processed_qc <- computeGearysC(results$processed, update_correspondence = TRUE)
  processed_qc <- computeSNR(processed_qc, update_correspondence = TRUE)
  plotQCOverview(x = processed_qc)
}
}
