% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preProcess.R
\name{preProcess}
\alias{preProcess}
\title{Pre-processing}
\usage{
preProcess(x, BPPARAM = BiocParallel::bpparam())
}
\arguments{
\item{x}{An unprocessed (raw) MSImagingExperiment object. Can be generated by loading in a .imzML file using Cardinal "readMSIData" function.}

\item{BPPARAM}{A \code{\link[BiocParallel]{BiocParallelParam}} object
controlling parallel execution. Defaults to
\code{\link[BiocParallel]{bpparam}()}, which uses the session-wide
registered backend. Use \code{BiocParallel::MulticoreParam(n)} on
Linux/macOS or \code{BiocParallel::SnowParam(n)} on Windows for
multi-core processing.}
}
\value{
A pre-processed MSImagingExperiment object.
}
\description{
Performs pre-processing steps on a raw MSImagingExperiment object. Steps include normalisation, spectral smoothing and baseline reduction.
}
\examples{
path <- system.file("extdata/Example_data/Example_data.imzML", package = "gutenTAG")
panel_path <- system.file("extdata/ref_list.csv", package = "gutenTAG")
panel <- readPanel(path = panel_path)
raw <- readMSIData(path)
preProcess(raw)
}
