% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotQC.R
\name{plotMetapeaks}
\alias{plotMetapeaks}
\title{Plot Metapeak Segmentation}
\usage{
plotMetapeaks(x, metapeaks, panel, interactive = FALSE)
}
\arguments{
\item{x}{List output of \code{assignMetapeaks}.}

\item{metapeaks}{List output of \code{generateMetapeaks}.}

\item{panel}{Data frame with columns \code{FeatureMass} and \code{Name}.
When loaded via \code{readPanel}, also contains \code{OriginalName}.}

\item{interactive}{Logical; if \code{TRUE} returns a \code{plotly} object
with mean and skyline spectra on a secondary y-axis. Requires the
\pkg{plotly} package. Default \code{FALSE}.}
}
\value{
A \code{ggplot} object, or a \code{plotly} object when
  \code{interactive = TRUE}.
}
\description{
Dual-axis plot showing the peak count histogram (line), smoothed
  counts, metapeak limit rectangles, panel feature masses (vertical lines),
  and optionally mean and skyline spectra overlaid on a secondary axis
  (interactive mode only, via \pkg{plotly}).
}
\examples{
rdata_path <- system.file("extdata/Example_data/Example_processed.Rdata", package = "gutenTAG")
load(rdata_path)
plotMetapeaks(x = results$processed, metapeaks = results$metapeaks,
              panel = results$panel)
}
